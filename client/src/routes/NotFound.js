const NotFound = () => {
  return (
    <div
      style={{
        height: "100vh",
        display: "flex",
        fontSize: "3rem",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <p>404</p>
    </div>
  );
};

export default NotFound;
